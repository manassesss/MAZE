import {StyleSheet} from 'react-native';

export default StyleSheet.create({

    container: {
        justifyContent: "center",
        padding: 17,
        marginTop: 30,
        marginBottom: 40,
        backgroundColor: "#fff"
      },
});