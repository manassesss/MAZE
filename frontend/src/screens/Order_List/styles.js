import { StyleSheet } from 'react-native';

export default StyleSheet.create({

  container: {
    justifyContent: "center",
    padding: 17,
    marginTop: 30,
    backgroundColor: "#fff",
    marginBottom: 45,
  },
  header: {
    color: "#525257", 
    marginBottom:10,
    fontSize: 30,
    fontWeight: "bold",
  }
});