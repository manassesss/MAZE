import {StyleSheet} from 'react-native';

export default StyleSheet.create({
      input: {
        height: 50,
        borderRadius: 10,
        borderColor: "#EECCAA",
        borderWidth: 1,
        marginBottom: 15,
      },
});