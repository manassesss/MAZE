import {StyleSheet} from 'react-native';

export default StyleSheet.create({
      textHeader: {
        fontSize: 20,
        fontWeight:"bold",
      },
      item: {
        borderColor: "#FCE8D4",
        borderWidth: 2,
        borderRadius: 15,
      },
      textItem : {
        color: "#91919F",
        fontWeight:"bold",
      }
});