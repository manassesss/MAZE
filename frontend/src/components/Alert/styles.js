import {StyleSheet} from 'react-native';

export default StyleSheet.create({

    container: {
        padding: 20,
        backgroundColor:"#fff",
        display: 'flex',
        justifyContent: "center",
        alignItems: "center"
        
      },
  
});